/***********************
* Adobe Edge Animate Composition Actions
*
* Edit this file with caution, being careful to preserve 
* function signatures and comments starting with 'Edge' to maintain the 
* ability to interact with these actions from within Adobe Edge Animate
*
***********************/
(function($, Edge, compId){
var Composition = Edge.Composition, Symbol = Edge.Symbol; // aliases for commonly used Edge classes

   //Edge symbol: 'stage'
   (function(symbolName) {
      
      
      

      

      

      Symbol.bindElementAction(compId, symbolName, "document", "compositionReady", function(sym, e) {
         $("#Stage").css("margin","auto")

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step2an}", "click", function(sym, e) {
         document.getElementById('step2').scrollIntoView();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step2an}", "mouseover", function(sym, e) {
         sym.$("step2an").css("opacity", "0.7");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step2an}", "mouseout", function(sym, e) {
         sym.$("step2an").css("opacity", "1.0");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step4an}", "click", function(sym, e) {
         document.getElementById('step4').scrollIntoView();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step4an}", "mouseover", function(sym, e) {
         sym.$("step4an").css("opacity", "0.7");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step4an}", "mouseout", function(sym, e) {
         sym.$("step4an").css("opacity", "1.0");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step3an}", "mouseover", function(sym, e) {
         sym.$("step3an").css("opacity", "0.7");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step3an}", "mouseout", function(sym, e) {
         sym.$("step3an").css("opacity", "1.0");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step3an}", "click", function(sym, e) {
         document.getElementById('step3').scrollIntoView();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step5an}", "click", function(sym, e) {
         document.getElementById('step5').scrollIntoView();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step5an}", "mouseover", function(sym, e) {
         sym.$("step5an").css("opacity", "0.7");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step5an}", "mouseout", function(sym, e) {
         sym.$("step5an").css("opacity", "1.0");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step6an}", "click", function(sym, e) {
         document.getElementById('step6').scrollIntoView();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step6an}", "mouseover", function(sym, e) {
         sym.$("step6an").css("opacity", "0.7");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step6an}", "mouseout", function(sym, e) {
         sym.$("step6an").css("opacity", "1.0");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step7an}", "click", function(sym, e) {
         document.getElementById('step7').scrollIntoView();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step7an}", "mouseover", function(sym, e) {
         sym.$("step7an").css("opacity", "0.7");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step7an}", "mouseout", function(sym, e) {
         sym.$("step7an").css("opacity", "1.0");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step8an}", "click", function(sym, e) {
         document.getElementById('step8').scrollIntoView();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step8an}", "mouseover", function(sym, e) {
         sym.$("step8an").css("opacity", "0.7");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step8an}", "mouseout", function(sym, e) {
         sym.$("step8an").css("opacity", "1.0");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step9an}", "click", function(sym, e) {
         document.getElementById('step9').scrollIntoView();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step9an}", "mouseover", function(sym, e) {
         sym.$("step9an").css("opacity", "0.7");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step9an}", "mouseout", function(sym, e) {
         sym.$("step9an").css("opacity", "1.0");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step10an}", "click", function(sym, e) {
         document.getElementById('step10').scrollIntoView();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step10an}", "mouseover", function(sym, e) {
         sym.$("step10an").css("opacity", "0.7");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step10an}", "mouseout", function(sym, e) {
         sym.$("step10an").css("opacity", "1.0");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step11an}", "click", function(sym, e) {
         document.getElementById('step11').scrollIntoView();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step11an}", "mouseover", function(sym, e) {
         sym.$("step11an").css("opacity", "0.7");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step11an}", "mouseout", function(sym, e) {
         sym.$("step11an").css("opacity", "1.0");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step12an}", "click", function(sym, e) {
         document.getElementById('step12').scrollIntoView();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step12an}", "mouseover", function(sym, e) {
         sym.$("step12an").css("opacity", "0.7");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step12an}", "mouseout", function(sym, e) {
         sym.$("step12an").css("opacity", "1.0");

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 2000, function(sym, e) {
         // insert code here
      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step1an}", "click", function(sym, e) {
         document.getElementById('step1').scrollIntoView();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step1an}", "mouseover", function(sym, e) {
         sym.$("step1an").css("opacity", "0.7");

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_step1an}", "mouseout", function(sym, e) {
         sym.$("step1an").css("opacity", "1.0");

      });
      //Edge binding end

   })("stage");
   //Edge symbol end:'stage'

})(jQuery, AdobeEdge, "EDGE-1045567121");